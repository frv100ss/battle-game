import React from "react";
import ButtonPlay from './App';
import App from "./App";

/*
describe('/Component/App', () => {
    it('Should Render App Component', () => {
        const wrapper = shallow(<App />);
        expect(wrapper).toExist();
    });

    it('Should Render Button Play Component', () => {
        const wrapper = shallow(<ButtonPlay />);
        expect(wrapper).toExist();

    });
});
*/

/*    it('Should contain Button PLay Component', () => {
        const wrapper = mount(<App />);
        expect(wrapper).to.containMatchingElement(<ButtonPlay className="btn-play" />)
    });*/
