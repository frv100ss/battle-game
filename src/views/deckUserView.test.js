import ButtonPlay from "./../controller/ButtonPlay"
import App from './App';

import React from 'react';

describe('Component Button Play ', () => {
/*    it('render without Exploding', () => {

        expect (
            shallow(
             <App />
            ).length
        ).toEqual(1);

    });

    it('should have a button inside', () => {
        const wrapper = shallow(<App />),
            btn = wrapper.find('button');

        expect(
            btn.length
        ).toEqual(1)

    });

    it('button inside should do something', ()=> {
        const wrapper = shallow(<ButtonPlay />),
            btn = wrapper.find('button');

            btn.simulate('click')
    })*/
});

